import styled from '@emotion/styled';

const Input = styled.input`
  border-radius: 2px;
  height: 30px;
  outline: none;
  font-size: 1rem;

  border-width: 1px;
  border-color: #b1afba;
`;

export default Input;
