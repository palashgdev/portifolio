import styled from '@emotion/styled';

const SlideImageWrapper = styled.div`
  background-color: #1d1f2f;
  border-radius: 1%;
  height: 100%;
  left: 0%;
  overflow: hidden;
  position: absolute;
  top: 0%;
  transition: transform calc(600ms / 4) 600ms;
  width: 100%;
`;

export default SlideImageWrapper;
